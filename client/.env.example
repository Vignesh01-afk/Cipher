# Optional. Leave empty in development: the Vite dev server proxies /api to
# http://localhost:4000 (see vite.config.ts).
#
# Set this when the client is served from a different origin than the API.
# VITE_API_BASE_URL="http://localhost:4000/api"
